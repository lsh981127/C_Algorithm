#include <stdio.h>
#include <stdlib.h>

void IsEmpty(int a[])
{

    if(a[] == 0)
        return true;
    else
        return false;

}
void IndexOf(char a[])
{

    for(int j = 0 ;j < 5; j++){
        if('x' == i[j])
            printf("%d\n", j);

    }

}
void Length(int a[])
{
    int i,b;
    if(sizeof a[i] == sizeof a[b]) //
        printf("%d", b);

    return void;
}
int main()
{
    char i[10];
    int n;
    int linear_arr[n] = {};


    for(int i = 0; i<n ;i++){
        printf("배열에 넣을 값을 입력하세요\n");
        printf("")
        if()
        scanf("%d");
    }
    IsEmpty(linear_arr);
    Length(linear_arr, );
    IndexOf()

    return 0;
}
